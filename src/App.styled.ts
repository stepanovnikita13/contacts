import { createUseStyles } from 'react-jss'

const useAppStyles = createUseStyles({
	wrapper: {
		width: "100%",
		height: "100vh",
		//overflow: "hidden"
	},
	container: {

	}
}, { name: 'app' })

export default useAppStyles